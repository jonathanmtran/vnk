type Query {
  queues: [Queues!]!
  youTubeVideos(query: String!): [YouTubeVideo!]!
}

type Queue {
  id: ID
  queueId: String
  name: String
  songName: String
  youTubeUrl: String
  created: String
  performed: String
}

type Queues {
  id: ID
  name: String
  created: String
}

type YouTubeVideo {
  id: String
  link: String
  title: String
}
